class ChatService {
    static sendMessage(message: string) {
        // Send the message to the chatbot server
        // and return the response
    }

    static receiveMessage() {
        // Receive a message from the chatbot server
        // and return it
    }
}

export default ChatService;