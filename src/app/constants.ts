const prefix = 'app';

export const CHECK_TOKEN = `${prefix}/CHECK_TOKEN`;
export const SET_TOKEN = `${prefix}/SET_TOKEN`;

export const SET_CURRENT_USER = `${prefix}/SET_CURRENT_USER`;

export const SET_HOME_PAGE = `${prefix}/SET_HOME_PAGE`;
export const SET_BUILD_DATA = `${prefix}/SET_BUILD_DATA`;

